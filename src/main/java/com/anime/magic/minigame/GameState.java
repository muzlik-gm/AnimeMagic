package com.anime.magic.minigame;

/**
 * State machine values for an arena instance.
 *   IDLE -> COUNTDOWN -> RUNNING -> ENDED -> IDLE
 */
public enum GameState {
    IDLE, COUNTDOWN, RUNNING, ENDED
}
